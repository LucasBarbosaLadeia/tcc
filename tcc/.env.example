# Variáveis de ambiente do backend local (desenvolvimento sem Docker)
# Copie este arquivo para .env e preencha com os valores do seu ambiente
# NUNCA commite o arquivo .env com senhas reais

DB_HOST=localhost
DB_PORT=3306
DB_USER=app
DB_PASSWORD=troque_essa_senha
DB_NAME=saude_na_mao
